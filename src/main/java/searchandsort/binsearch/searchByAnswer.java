package searchandsort.binsearch;

public class searchByAnswer {
    private int binarySearch(int w, int h, int n) {
        int shortest;
        int longest;
        if (w != h) {
            longest = w > h ? w : h;
            shortest = w < h ? w : h;
        }
        else {
            shortest=w;
            longest=h;
        }
    return -1;
    }
}
